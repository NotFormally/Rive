# Deploy Workflow
1. Run `npm run build` and fix any errors
2. Run `npm run lint` and fix warnings
3. Check for hardcoded env vars or secrets
4. Push to main branch
5. Verify Vercel deployment status
